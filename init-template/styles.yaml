# styles.yaml is used to customize various text styles.
# 'format' values are processed using Handlebars template engine.

reference:
  format: >
    {{authors}} ({{issue.year}}) {{title}}
citation:
  format: >
    [{{{items}}}]
  itemSep: ','
  hyphen: '-'
